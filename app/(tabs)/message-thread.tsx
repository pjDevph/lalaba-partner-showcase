// app/(tabs)/message-thread.tsx
// Thin route wrapper — see src/screens/providerChat/ProviderMessageThreadScreen.tsx.
// The merchant's own PROVIDER-kind chat with a customer (brand-blue accent,
// derived from the (tabs) route group). Distinct from app/(tabs)/courier-thread.tsx,
// which is the courier-style leg chat for a self-run pickup/return.
export { default } from "../../src/screens/providerChat/ProviderMessageThreadScreen";
