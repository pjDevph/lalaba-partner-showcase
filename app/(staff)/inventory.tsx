// app/(staff)/inventory.tsx
// Staff inventory tab — re-uses the merchant inventory screen.
// Backend already allows STAFF role on all /inventory routes.
export { default } from "../(tabs)/inventory";
