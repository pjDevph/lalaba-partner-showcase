// app/(staff)/message-thread.tsx
// Thin route wrapper — the staff-stack copy of the provider's customer thread.
// Same screen the owner uses; the stack differs so a staff member never has to
// enter (tabs) to answer a message.
export { default } from "../../src/screens/providerChat/ProviderMessageThreadScreen";
