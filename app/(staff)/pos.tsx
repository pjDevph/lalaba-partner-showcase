// app/(staff)/pos.tsx
// Staff POS tab — renders the same POS as the merchant owner.
// Services are loaded from the API; branch context comes from authStore.
export { default } from "../(tabs)/pos";
